# Releases ItLingo

Les binaires APK ne sont pas versionnes dans Git.

Pour regenerer l'APK:

```bash
cd ../frontend
flutter build apk --release
```

Fichier produit:

```text
../frontend/build/app/outputs/flutter-apk/app-release.apk
```
